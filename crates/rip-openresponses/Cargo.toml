[package]
name = "rip-openresponses"
version = "0.1.0"
edition = "2021"

[dependencies]
jsonschema = "0.17"
once_cell = "1.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_json = "1.0"
